###
### GitHub repository .gitignore section
###

# Ignore directories generated by Composer
/drush/contrib/
/drush/Commands/contrib/
/vendor/
/web/core/
# needed for GitKraken bug.
/web/core/**
/web/libraries
/web/modules/contrib/
/web/themes/contrib/
/web/profiles/contrib/
/web/private/scripts/quicksilver
/web/libraries/

# Ignore scaffold files
web/.csslintrc
web/.editorconfig
web/.eslintignore
web/.eslintrc
web/.eslintrc.json
web/.gitattributes
web/.ht.router.php
web/.htaccess
web/autoload.php
web/index.php
web/robots.txt
web/sites/default/default.services.pantheon.preproduction.yml
web/sites/default/default.services.yml
web/sites/default/default.settings.php
web/sites/default/settings.pantheon.php
web/sites/development.services.yml
web/sites/example.settings.local.php
web/sites/example.sites.php
web/update.php
web/web.config
/.drush-lock-update
/.gitattributes
/.editorconfig
.phpunit.result.cache

# Add directories containing build assets below.
# Keep all additions above the "cut" line.

# This distinction is only important when using this
# repository as a custom upstream. The .gitignore file
# is not modified in the GitHub PR workflow.


# :::::::::::::::::::::: cut ::::::::::::::::::::::

###
### Pantheon site .gitignore section
###
### Items below the "cut" line are still ignored on
### the Pantheon site. Items above the "cut" line
### are ignored in the GitHub repository, but committed
### to the Pantheon repository.
###

# Ignore Drupal's file directory
web/sites/default/files

# Pantheon commits a settings.php for environment-specific settings.
# Place local settings in settings.local.php
web/sites/*/settings.local.php
web/sites/*/services*.yml
!web/sites/*/services.pantheon.preproduction.yml
!web/sites/*/services.pantheon.production.yml

# Ignore SimpleTest multi-site environment.
web/sites/simpletest

# Ignore files generated by PhpStorm
.idea

# Packages #
############
*.7z
*.dmg
*.gz
*.bz2
*.iso
*.jar
*.rar
*.tar
*.zip
*.tgz

# Logs and databases #
######################
*.log
*.sql

# OS generated files #
######################
.DS_Store*
ehthumbs.db
Thumbs.db
._*

# Vim generated files #
######################
*.un~

# VS Code generated files #
###########################
.vscode/*

# Nova Code generated files #
###########################
.nova/*

# SASS #
##########
.sass-cache

# Things in the core directory that Drupal 8 commits in the repository.
!web/core/**/*.gz

# Ignore (obsolete) drushrc.php just in case anyone still has one lying around.
web/sites/*/drushrc.local.php
# Ignore drush.yml since we only use it for environment-specific settings.
drush/drush.yml

# We've also added a services yml for environment-specific services.
web/sites/*/default.services.local.yml

# hide adminimal_theme until approved by team.
web/themes/dev/adminimal_theme

# Drupal Console cache artifacts
console/cache/

# hide eck feature as dev modules only needed for scaffolding new entities.
web/modules/dev/eck
web/modules/dev/features
web/modules/dev/config_update
web/sites/default/config/features.*

# ignore (ill-placed) libraries directory added by leaflet dependency
/libraries/

# ignore local copy of .env
.env

# ignore local copy of phpunit.xml
tests/phpunit.xml
tests/.phpunit.result.cache

# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc